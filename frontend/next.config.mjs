/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["example.com" , "m.media-amazon.com"], // Add the domain hosting your images
  },
};

export default nextConfig;
